---
import Layout from "@layouts/Layout.astro";
import { SolidApp } from "../components/SolidApp";

interface Props {
    title: string;
    id: string;
}

const { id } = Astro.props;
---

<Layout title="Panel">
    <SolidApp property={id} client:load />
</Layout>
